# create a books_api blueprint that serves the following routes:
# GET /api/v1/books - return a list of books
# POST /api/v1/books - create a new book

# Environment variables are already loaded using load_dotenv() method in main file.
# Read the environment variables from the .env file to the following variables:
# MONGO_CONNECTION_STRING, MONGO_DATABASE, MONGO_COLLECTION

# create a MongoClient object with the MONGO_CONNECTION_STRING
# get the database and collection objects from the MongoClient object

# create a get_blueprint function that returns the books_api blueprint




