# create a flast api application that serves the books_api blueprint on /api/v1/books
# use the python-dotenv library to load the environment variables from the .env file
# enable CORS on the application
# create error handlers for 400, 404 and 500 errors
# run the application if the script is executed directly



